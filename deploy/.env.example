# Docker is used only for the local PostgreSQL database.
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=54329

# Host-process backend settings (used by scripts/dev/start-backend.sh).
WORKER_CONCURRENCY=2
ACCESS_TOKEN=
MCP_TOKEN=
